dependencies {
    implementation("io.vizend:accent:${accentVersion}")
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = group
            artifactId = project.name
            version = version
            from components.java
        }
    }
}